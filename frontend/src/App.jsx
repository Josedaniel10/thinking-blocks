import { useEffect, useState } from "react"
import { getAllThoughts } from "./services/thoughts"

function App() {
  const [data, setData] = useState(null)

  return (
    <div className="w-full h-full grid auto-cols-[minmax(80px,1fr)] gap-0.5">
      
    </div>
  )
}

export default App
